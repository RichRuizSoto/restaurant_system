<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Inicio de sesión</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/system/login.css" />
  <style>
  body {
    background-color: #272727;
  }
</style>

</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <i class="bi bi-shield-lock-fill"></i>
      <h2>Iniciar Sesión</h2>
    </div>

    <% if (typeof error !== 'undefined') { %>
      <div class="error-message"><%= error %></div>
    <% } %>

    <!-- Aquí removemos el action y el método POST -->
    <form id="loginForm" autocomplete="off">
      <input type="hidden" name="returnTo" id="returnToField" value="">

  <!-- Contenedor para errores generales -->
<div id="formErrorContainer"></div>

      <div class="form-group">
        <label for="nombre">Nombre de usuario</label>
        <i class="bi bi-person"></i>
        <input type="text" name="nombre" id="nombre" required placeholder="usuario" />
      </div>

      <div class="form-group">
        <label for="clave">Contraseña</label>
        <i class="bi bi-lock"></i>
        <input type="password" name="clave" id="clave" required placeholder="********" />
      </div>

      <div class="form-group full-width">
        <label for="rol">Rol</label>
        <i class="bi"></i>
        <select name="rol" id="rol" required>
          <option value="">Selecciona un rol</option>
          <option value="admin">Administrador</option>
          <option value="gestor">Gestor</option>
          <option value="empleado">Empleado</option>
        </select>
      </div>

      <div class="form-group full-width">
        <button type="submit" class="login-button">Ingresar</button>
      </div>

      <div class="footer">
        ¿Olvidaste tu contraseña? <a href="#">Recupérala</a>
      </div>
    </form>
  </div>

  <script src="/scripts/login.js"></script>
</body>
</html>
